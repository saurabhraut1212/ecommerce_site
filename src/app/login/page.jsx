import React from 'react';
import LoginForm from '../../pages_section/Login';

export const metadata = {
	title: 'Login Form',
	description: 'Login for the ecommerce website',
};

const Login = () => {
	return <LoginForm />;
};

export default Login;
